# Input para otimização de geometria DFT do cluster de Triptofanos
# Arkhe-Block 2026-TDDFT-EXCITON

! ωB97X-D3 def2-SVP TightOpt CPCM(ε=4) NormalPrint
%pal nprocs 32 end
%maxcore 8000

%geom
  # Congelar os átomos de Cβ para manter a geometria biológica
  Constraints
    { C 1 } C  # Ajustar índices conforme o ficheiro XYZ gerado
    { C 2 } C
    # ... (lista completa dos Cβ)
  end
end

* xyzfile 0 1 tryptophan_cluster.xyz
