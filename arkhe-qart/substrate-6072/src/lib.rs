pub mod provenance {
    pub mod zk_circuit;
}
pub mod compensation {
    pub mod x402_pix_bridge;
}
pub mod fingerprint {
    pub mod perceptual;
}
pub mod oracle {
    pub mod entropy_auction;
    pub mod trend_detector;
}
pub mod influence {
    // probability_model mock deleted, so module intentionally removed or kept out of tree
}
